import { FETCH_TACOS, ADDDATABASETACOS } from '../actions';

const initialState = {
  tacos: []
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
